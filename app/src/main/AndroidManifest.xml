<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gymplanapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymPlanApp">

        <!-- MainActivity as the entry point of the app -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".WorkoutPlansActivity" android:exported="true" />
        <activity android:name=".DietPlansActivity" android:exported="true" />
        <activity android:name=".ProgressTrackerActivity" android:exported="true" />
        <activity android:name=".UserDetailsActivity" android:exported="true" />

        <!-- Diet Plans -->
        <activity android:name=".PlanAActivity" android:exported="true" />
        <activity android:name=".PlanBActivity" android:exported="true" />
        <activity android:name=".PlanCActivity" android:exported="true" />

    </application>

</manifest>
